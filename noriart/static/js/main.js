var option = {
    section : '.js-section',
    easing: "swing",
    scrollSpeed: 600,
    scrollbars: true,
};

// $.scrollify({section:".section"});

$(function() {
    $.scrollify(option); // scrollifyの実行
  });